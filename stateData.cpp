#include "stateData.h"
#include <iomanip>

// Constructor
stateData::stateData(string nm, string cap, double g, string mot, string fl, double ar)
    : name(nm), capital(cap), gdp(g), motto(mot), flower(fl), area(ar) {}

// The rest of the code ...
